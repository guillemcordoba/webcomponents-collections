import { storiesOf } from '@storybook/polymer';
import { html } from 'lit-html';

// import '../filtering/components/single/filter-string';

storiesOf('Demo', module).add(
  'lit-html story',
  () => html`
    <button>sdf</button>
  `
);
